# Published evidence

No GPU result is published yet.

Add a result only after the complete remote evidence index reports `passed`, every referenced hash verifies, seeded defects fail for the intended reason, nearby controls pass, and model redistribution review is complete.

Every published numerical or performance statement must link to a checked-in small machine-readable table.
Do not commit engines, raw output tensors, timing caches, `.nsys-rep`, `.ncu-rep`, or restricted model bytes.
