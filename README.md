# TensorRT UpgradeGuard

TensorRT UpgradeGuard is a host-controlled release-qualification laboratory for TensorRT stack upgrades.

The repository is under active implementation against [BUILD_PLAN.md](BUILD_PLAN.md).
The current milestone is hardware and compatibility locking.
No GPU qualification or performance claim is published until its real-hardware gate passes.
