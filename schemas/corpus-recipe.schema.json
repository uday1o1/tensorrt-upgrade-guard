{
  "$comment": "Generated by scripts/generate_schemas.py. Do not edit manually.",
  "$defs": {
    "TransformerShape": {
      "additionalProperties": false,
      "description": "One concrete transformer shape.",
      "properties": {
        "batch": {
          "maximum": 8,
          "minimum": 1,
          "title": "Batch",
          "type": "integer"
        },
        "id": {
          "title": "Id",
          "type": "string"
        },
        "sequence": {
          "maximum": 512,
          "minimum": 8,
          "title": "Sequence",
          "type": "integer"
        },
        "weight": {
          "exclusiveMinimum": 0,
          "maximum": 1,
          "title": "Weight",
          "type": "number"
        }
      },
      "required": [
        "id",
        "batch",
        "sequence",
        "weight"
      ],
      "title": "TransformerShape",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Authored, reviewable corpus recipe.",
  "properties": {
    "api_version": {
      "const": "upgradeguard.dev/v1alpha1",
      "title": "Api Version",
      "type": "string"
    },
    "expected_model_sha256": {
      "additionalProperties": {
        "pattern": "^sha256:[0-9a-f]{64}$",
        "type": "string"
      },
      "propertyNames": {
        "enum": [
          "fp32",
          "fp16"
        ]
      },
      "title": "Expected Model Sha256",
      "type": "object"
    },
    "generator_version": {
      "const": "tiny-transformer-v1",
      "title": "Generator Version",
      "type": "string"
    },
    "id": {
      "title": "Id",
      "type": "string"
    },
    "kind": {
      "const": "CorpusRecipe",
      "title": "Kind",
      "type": "string"
    },
    "precisions": {
      "items": {
        "enum": [
          "fp32",
          "fp16"
        ],
        "type": "string"
      },
      "title": "Precisions",
      "type": "array"
    },
    "transformer_shapes": {
      "items": {
        "$ref": "#/$defs/TransformerShape"
      },
      "title": "Transformer Shapes",
      "type": "array"
    }
  },
  "required": [
    "api_version",
    "kind",
    "id",
    "generator_version",
    "precisions",
    "transformer_shapes",
    "expected_model_sha256"
  ],
  "title": "CorpusRecipe",
  "type": "object"
}
